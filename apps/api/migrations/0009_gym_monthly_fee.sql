-- ジムの月額料金（user_profile へ列を1つ足す）。
--
-- 「今月は何回行って、1回あたりいくらか」をホームに出すために持つ。
-- 恒常的に持つ属性なので 0007 の user_profile へ入れる（期間ごとに変わる値ではない）。
--
-- NULL 可。未設定なら「今月のジム代」の表示そのものを出さない
-- （使っていない人に機能を見せない。予定の表示と同じ扱い）。
-- 端末の入力欄は数値だけを扱うため 0 を未入力として NULL へ寄せる（height_cm と同じ流儀）。
--
-- 単位は円で整数。為替や他通貨は扱わない（そのときは通貨の列を足して決める）。

ALTER TABLE user_profile ADD COLUMN gym_monthly_fee_yen INTEGER;
