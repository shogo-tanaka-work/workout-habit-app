# ローカル開発（npm run dev）で API リクエストの転送先にする、デプロイ済み Worker のオリジン。
# このファイルを .env.local へコピーして実値を設定する。.env.local はコミットしない。
# 本番は apps/api の static assets として同一オリジン配信されるため、この設定は使われない。
VITE_API_ORIGIN=https://workout-habit-api.<your-subdomain>.workers.dev
